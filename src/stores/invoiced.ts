import { useAxios } from "@/axios/axios";
import { defineStore } from "pinia";

export const useInvoiceStore = defineStore("invoice", () => {
  async function generateInvoice(clientID: number) {
    try {
    } catch (error) {}
  }

  return {};
});
